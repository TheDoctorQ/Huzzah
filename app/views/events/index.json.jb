@events.map do |event|
  {
    id: event.id,
    title: event.title,
    time: event.time,
    location: event.location,
    images: event.images,
    created_at: event.created_at,
    updated_at: event.updated_at
  }
end